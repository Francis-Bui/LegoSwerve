const float radius = 10.0;
const float trackWidth = 10.0; //radius * cos(pi/4);
const float wheelBase = 10.0; //radius * sin(pi/4)
const int PID_SENSOR_INDEX = S1;
const int IRreceiver = S2;
const int PID_MOTOR_INDEX = motorA;
